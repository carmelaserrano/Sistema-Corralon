// Stub de contrato — implementación real en S3-12.
// Tabla: ventas (0031_base_sprint3.sql).

/** @param {Object} [filtros] @returns {Promise<Array<Object>>} */
export async function listarVentas() {
  throw new Error('No implementado — ver issue S3-12')
}

/** @param {string} id @returns {Promise<Object>} Venta con detalle, comprobantes y cobros. */
export async function getVentaById() {
  throw new Error('No implementado — ver issue S3-12')
}
