// Stub de contrato — implementación real en S3-14.
// Vista: v_catalogo_web, accesible a `anon` (0031_base_sprint3.sql).

/** @param {Object} [filtros] {search, categoriaId, marcaId, orden, pagina} @returns {Promise<Array<Object>>} */
export async function listarCatalogo() {
  throw new Error('No implementado — ver issue S3-14')
}

/** @param {string} productoId @returns {Promise<Object>} */
export async function getProductoWeb() {
  throw new Error('No implementado — ver issue S3-14')
}
